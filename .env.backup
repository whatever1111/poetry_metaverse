# 服务器配置
PORT=3000
NODE_ENV=production

# 管理员账号配置
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Poetry@Admin2024

# Session密钥 - 请更改为一个复杂的随机字符串
SESSION_SECRET=$(openssl rand -base64 64 | tr -d "
")

# 域名配置（部署时修改）
DOMAIN=localhost

# 安全配置
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
